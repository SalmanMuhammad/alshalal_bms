# Local backend
MONGODB_URI=mongodb://localhost:27017/alshalal-factory
JWT_SECRET=change-this-to-a-strong-secret
PORT=5001
HOST=0.0.0.0

# Local frontend dev proxy target
VITE_PROXY_TARGET=http://localhost:5001

# Production frontend target (set in Cloudflare Pages, not in local .env by default)
# VITE_API_URL=https://your-render-service.onrender.com/api
